[project]
name = "aiagentexperiment-server"
version = "0.1.0"
description = "Authoritative backend for simulation, cognition, and persistence"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
